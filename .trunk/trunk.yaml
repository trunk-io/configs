version: 0.1
cli:
  version: 1.20.1
plugins:
  sources:
    - id: trunk
      uri: https://github.com/trunk-io/plugins
      ref: v1.4.5

    - id: configs
      local: .
# see plugin.yaml for enabled linters
lint:
  disabled:
    - eslint
#   enabled: []
