version: 0.1
cli:
  version: 1.16.0
plugins:
  sources:
    - id: trunk
      uri: https://github.com/trunk-io/plugins
      ref: v1.2.3

    - id: configs
      local: .
# see plugin.yaml for enabled linters
# lint:
#   enabled: []
