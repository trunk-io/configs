version: 0.1
cli:
  version: 1.12.3
plugins:
  sources:
    - id: trunk
      ref: v1.0.0
      uri: https://github.com/trunk-io/plugins

    - id: configs
      local: .
# see plugin.yaml for enabled linters
# lint:
#   enabled: []
