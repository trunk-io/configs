version: 0.1
cli:
  version: 1.22.2
plugins:
  sources:
    - id: trunk
      uri: https://github.com/trunk-io/plugins
      ref: v1.6.2

    - id: configs
      local: .
# see plugin.yaml for enabled linters
lint:
  disabled:
    - eslint
#   enabled: []
